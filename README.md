# Test drive to GO programing language

Keep is a API to manage keeps, project in development.

## Instalation

On Linux, clone this repository and execute command:

> go run $(ls -1 *.go | grep -v _test.go) 

